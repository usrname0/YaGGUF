# Core dependencies
huggingface-hub>=0.20.0
numpy>=1.24.0
gguf>=0.1.0

# For model conversion (llama.cpp convert script)
# Note: PyTorch is installed separately by setup scripts (CPU-only version)
# If installing manually: pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
transformers>=4.30.0
sentencepiece>=0.1.99

# Optional: For GUI
streamlit>=1.30.0
